import { DragDropImageDirective } from './drag-drop-image.directive';

describe('DragDropImageDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropImageDirective();
    expect(directive).toBeTruthy();
  });
});
